using System;

namespace Program
{
    class main
    {
        static void Main(string[] args)
        {
            var arr = from i in Enumerable.Range(10) select number;
            Console.WriteLine(arr);
        }
    }
}